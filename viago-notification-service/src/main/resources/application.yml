spring :
  application:
    name: viago-notification-service
  profiles :
    active : dev

server :
  port : ${SERVER_PORT}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/